package domine;

/**
 * Enum que representa los diferentes tipos de trámites que un estudiante puede solicitar.
 * @author Jaime Landázuri
 * */
public enum ProcedureType {
    CERTIFICADO,
    MATRICULA,
    HOMOLOGACION,
    RETIRO_ASIGNATURA,
    OTRO;
}
